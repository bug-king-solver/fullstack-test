import { Container } from "@mui/material";
import { ArticleList } from "../components/pages";

const HomePage = () => {
  return (
    <Container maxWidth="lg">
      <ArticleList />
    </Container>
  );
};

export default HomePage;
